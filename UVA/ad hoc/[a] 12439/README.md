This problem has a lot of memories!

Run the Unit Tests:

`g++ unittest.cpp -o unittest.exe && ./unittest.exe`

Run the code on input.txt and write to output.txt

`g++ leapday.cpp -o leapday.exe  && ./leapday.exe < input.txt > output.txt`